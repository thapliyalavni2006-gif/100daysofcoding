// Q41: Write a program to swap the first and last digit of a number.
#include <stdio.h>
#include <math.h>
int main()
{
	int n, first, end, swap, digit, divide;
	printf("enter number ");
	scanf("%d", &n);
	digit = log10(n);
}